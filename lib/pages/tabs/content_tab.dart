import 'package:flutter/material.dart';

class ContentTabPage extends StatelessWidget {
  const ContentTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('内容项页面'),
    );
  }
} 