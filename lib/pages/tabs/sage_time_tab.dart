import 'package:flutter/material.dart';

class SageTimeTabPage extends StatelessWidget {
  const SageTimeTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('贤者时间页面'),
    );
  }
} 